#pragma once

// Cote gauche/droit du robot, partage entre les moteurs et les encodeurs
// pour indexer leurs tableaux respectifs de maniere coherente.
enum {
    LEFT = 0,
    RIGHT = 1,
    MOT_NUM
};
